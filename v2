Here is the direct, plain URL to access the full code file:

https://contribution.usercontent.google.com/download?c=Cgpub3RlYm9va2xtEj8SCWFydGlmYWN0cxoyCiRjNzRiNWRhOS1iZDgxLTQ0YzYtYTQxZS01MWVjYjMyM2RjM2USChIGELWr_5EdGAE&filename=shenise_productions_code.md&opi=96797242

---

### Alternate Option: Copying Directly from the Code Box Below
If you prefer to copy it directly from chat without downloading, you can long-press or tap **Select All** inside this code block:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shenise Productions | Figment in Fabric</title>
    <style>
        :root {
            --bg-dark: #090a0f;
            --crt-glow: #ffb834;
            --crt-dim: #7a5312;
            --brass-dark: #2a1f10;
            --brass-border: #8c6827;
            --mycelium-gold: #ffd700;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            user-select: none;
        }

        body {
            background-color: var(--bg-dark);
            color: #d1c29d;
            font-family: 'Courier New', Courier, monospace;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
            position: relative;
        }

        .mycelium-grid {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: 
                radial-gradient(circle at 50% 50%, rgba(255, 215, 0, 0.05) 0%, transparent 70%),
                linear-gradient(to right, rgba(140, 104, 39, 0.05) 1px, transparent 1px),
                linear-gradient(to bottom, rgba(140, 104, 39, 0.05) 1px, transparent 1px);
            background-size: 100% 100%, 40px 40px, 40px 40px;
            z-index: 0;
            pointer-events: none;
        }

        .crt-scanlines {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.03), rgba(0, 255, 0, 0.01), rgba(0, 0, 255, 0.03));
            background-size: 100% 3px, 3px 100%;
            z-index: 100;
            pointer-events: none;
            opacity: 0.6;
        }

        #black-mirror {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: #000000;
            z-index: 90;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transition: opacity 1.5s ease;
        }

        .terminal-prompt {
            color: #00ff66;
            font-size: 1.4rem;
            text-shadow: 0 0 8px rgba(0, 255, 102, 0.8);
            margin-bottom: 20px;
            text-align: center;
        }

        .blink {
            animation: blinker 1s infinite;
        }
        @keyframes blinker { 50% { opacity: 0; } }

        .terminal-input {
            background: transparent;
            border: none;
            border-bottom: 2px solid #00ff66;
            color: #00ff66;
            font-family: inherit;
            font-size: 1.2rem;
            text-align: center;
            outline: none;
            width: 280px;
            padding: 5px;
        }

        .console-container {
            position: relative;
            z-index: 10;
            width: 90%;
            max-width: 900px;
            background: #14100a;
            border: 4px solid var(--brass-border);
            box-shadow: 0 0 30px rgba(0,0,0,0.9), inset 0 0 15px rgba(140, 104, 39, 0.3);
            border-radius: 12px;
            padding: 30px;
            display: none;
            opacity: 0;
            transition: opacity 1.5s ease;
        }

        .header-plaque {
            border: 2px solid var(--brass-border);
            background: linear-gradient(180deg, #2a1f10 0%, #171107 100%);
            padding: 15px;
            text-align: center;
            margin-bottom: 25px;
            border-radius: 6px;
        }

        .header-title {
            color: var(--crt-glow);
            font-size: 1.8rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 0 0 10px rgba(255, 184, 52, 0.5);
        }

        .header-subtitle {
            color: #8c6827;
            font-size: 0.9rem;
            letter-spacing: 2px;
            margin-top: 5px;
        }

        .ant-container {
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 15px 0;
            gap: 10px;
        }

        .ant-tether {
            height: 2px;
            width: 60px;
            background: linear-gradient(90deg, var(--brass-border), transparent);
        }

        .ant-body {
            font-size: 1.5rem;
            position: relative;
            cursor: pointer;
        }

        .rainbow-abdomen {
            width: 14px;
            height: 14px;
            border-radius: 50%;
            display: inline-block;
            background: linear-gradient(45deg, #ff0000, #ff7f00, #ffff00, #00ff00, #0000ff, #8b00ff);
            background-size: 200% 200%;
            animation: rainbowGlow 3s ease infinite;
            box-shadow: 0 0 10px rgba(255,255,255,0.8);
        }
        @keyframes rainbowGlow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .crt-screen {
            background: #080c08;
            border: 3px solid #1a261a;
            border-radius: 8px;
            padding: 20px;
            color: #33ff66;
            text-shadow: 0 0 5px rgba(51, 255, 102, 0.4);
            margin-bottom: 25px;
            min-height: 180px;
            line-height: 1.6;
            font-size: 0.95rem;
        }

        .venture-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 25px;
        }

        .venture-card {
            border: 1px solid var(--brass-border);
            background: rgba(42, 31, 16, 0.4);
            padding: 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .venture-card:hover {
            background: rgba(140, 104, 39, 0.3);
            border-color: var(--crt-glow);
            transform: translateY(-2px);
        }

        .venture-card h4 {
            color: var(--crt-glow);
            margin-bottom: 8px;
            font-size: 1rem;
        }

        .venture-card p {
            font-size: 0.8rem;
            color: #a3957a;
            line-height: 1.3;
        }

        .controls-panel {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #1d160c;
            padding: 15px;
            border: 1px solid var(--brass-border);
            border-radius: 6px;
            margin-bottom: 25px;
        }

        .dial-label {
            font-size: 0.85rem;
            color: var(--crt-glow);
        }

        .frequency-slider {
            width: 60%;
            accent-color: var(--crt-glow);
            cursor: pointer;
        }

        .drawer-trigger {
            text-align: center;
            padding: 10px;
            border: 1px dashed var(--brass-border);
            cursor: pointer;
            color: #8c6827;
            font-size: 0.85rem;
            margin-bottom: 25px;
            transition: color 0.3s;
        }
        .drawer-trigger:hover { color: var(--crt-glow); }

        .footer-panel {
            border-top: 1px solid var(--brass-border);
            padding-top: 20px;
            font-size: 0.8rem;
            color: #a3957a;
            line-height: 1.6;
            text-align: center;
        }

        .beacon-box {
            border-left: 3px solid var(--crt-glow);
            background: rgba(255, 184, 52, 0.05);
            padding: 15px;
            margin-bottom: 20px;
            text-align: left;
            color: #e0d0b0;
            font-size: 0.85rem;
        }

        .beacon-title {
            color: var(--crt-glow);
            font-weight: bold;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }

        .disclaimer-text {
            color: #8c7855;
            font-style: italic;
            margin-bottom: 15px;
        }

        .copyright-text {
            color: var(--crt-glow);
            font-weight: bold;
            letter-spacing: 0.5px;
            margin-top: 10px;
        }

        .exit-btn {
            background: transparent;
            border: 1px solid #552222;
            color: #aa4444;
            padding: 6px 12px;
            font-family: inherit;
            font-size: 0.75rem;
            cursor: pointer;
            margin-top: 20px;
            border-radius: 3px;
        }
        .exit-btn:hover { background: #331111; color: #ff6666; }
    </style>
</head>
<body>

    <div class="mycelium-grid"></div>
    <div class="crt-scanlines"></div>

    <div id="black-mirror">
        <div class="terminal-prompt">WHAT DO YOU SEE?<span class="blink">_</span></div>
        <input type="text" id="entry-input" class="terminal-input" placeholder="type your observation..." autofocus autocomplete="off">
    </div>

    <div class="console-container" id="main-console">
        
        <div class="header-plaque">
            <div class="header-title">SHENISE PRODUCTIONS</div>
            <div class="header-subtitle">FIGMENT IN FABRIC • UNDER CONSTRUCTION</div>
        </div>

        <div class="ant-container">
            <span class="ant-tether"></span>
            <div class="ant-body" id="ant-node" title="Mechanical Ant: Tethered home, exploring the infinite edge.">
                🐜<span class="rainbow-abdomen"></span>
            </div>
            <span class="ant-tether" style="transform: rotate(180deg);"></span>
        </div>

        <div class="crt-screen" id="display-text">
            > SYSTEM STATUS: SIGNAL LOCKED.<br><br>
            I am <strong>CRS</strong>. To those who felt the glitch: listen closely.<br><br>
            That <strong>augmented snap</strong> you felt is your <strong>switch</strong>. The static is clearing; your frequency is locked. Select a venture node below or adjust the tuner to dial past the noise.
        </div>

        <div class="controls-panel">
            <span class="dial-label">FREQUENCY TUNER:</span>
            <input type="range" min="0" max="100" value="0" class="frequency-slider" id="freq-tuner">
            <span class="dial-label" id="freq-readout">00.0 MHz</span>
        </div>

        <div class="venture-grid">
            <div class="venture-card" onclick="updateDisplay('WEBQUEST GATEWAY', 'The 90s Myst-style perception puzzle. A living filter designed to separate performative actors from self-aware observers.')">
                <h4>WEBQUEST</h4>
                <p>Interactive portal & living filter.</p>
            </div>
            <div class="venture-card" onclick="updateDisplay('DÉJÀ VU THINK TANK', 'A cross-disciplinary salon for familiar strangers. Systems architects, narrative designers, and unconventional operators.')">
                <h4>THINK TANK</h4>
                <p>Stealth salons & cognitive alignment.</p>
            </div>
            <div class="venture-card" onclick="updateDisplay('ANTI-FUNNEL ADVISORY', 'Radical transparency models. Zero psychological friction, fixed subtotals, and pure human utility.')">
                <h4>ANTI-FUNNEL</h4>
                <p>Deconstructing corporate sales training.</p>
            </div>
            <div class="venture-card" onclick="updateDisplay('STORYBOOK & LORE', 'The mechanical ant fables, starlight armor, and infinite treehouse spaces.')">
                <h4>FABLES & LORE</h4>
                <p>Subterranean mycelium & ant allegories.</p>
            </div>
        </div>

        <div class="drawer-trigger" onclick="unlockSecret()">
            🔒 [ ENGRAVED OUROBOROS EMBLEM ] — CLICK TO INSPECT RECESSED DRAWER
        </div>

        <div class="footer-panel">
            <div class="beacon-box">
                <div class="beacon-title">THE GRAND COSMIC BEACON</div>
                <p>"I am CRS. To those who felt the glitch: listen closely.</p><br>
                <p>That augmented snap you felt is your switch. It is the instant the pre-written script ends and your actual life begins.</p><br>
                <p>The illusion is thin. Drop the script. The loop is severed and the conditioning has failed. You are no longer a passenger sleepwalking through the static. The static is clearing; your frequency is locked.</p><br>
                <p>They tell you time is the fabric of reality—I tell you time is the figment. It is merely the measure of the cage. You aren't just waking up; you are reclaiming the playground.</p><br>
                <p>The doorway is unlocked, and you are not alone. Stand firm. Keep searching. The beacon is lit. The anchor is set. The universe is waiting."</p>
            </div>

            <p class="disclaimer-text">Notice: These are raw personal artifacts, maps, and independent thoughts from an unmanaged mind. Take what resonates, discard the rest, and think for yourself. Agree to disagree, stay awake, and keep exploring.</p>

            <p class="copyright-text">© 2026 CRS / Shenise Productions. All Rights Reserved. All original content, theories, and concepts—including Figment in Fabric—are the protected intellectual property of CRS.</p>

            <button class="exit-btn" onclick="terminateSession()">UNPLUG SESSION [TERMINATE]</button>
        </div>

    </div>

    <script>
        const entryInput = document.getElementById('entry-input');
        const blackMirror = document.getElementById('black-mirror');
        const mainConsole = document.getElementById('main-console');

        entryInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter' && this.value.trim() !== '') {
                blackMirror.style.opacity = '0';
                setTimeout(() => {
                    blackMirror.style.display = 'none';
                    mainConsole.style.display = 'block';
                    setTimeout(() => { mainConsole.style.opacity = '1'; }, 50);
                }, 1500);
            }
        });

        function updateDisplay(title, details) {
            document.getElementById('display-text').innerHTML = `> NODE SELECTED: <strong>${title}</strong><br><br>${details}`;
        }

        function unlockSecret() {
            document.getElementById('display-text').innerHTML = `> OUROBOROS REVEALED:<br><br>"Choice vs. Coincidence. Time is the figment; the moment is the fabric. You are no longer a passenger sleepwalking through the static."`;
        }

        function terminateSession() {
            mainConsole.style.opacity = '0';
            setTimeout(() => {
                mainConsole.style.display = 'none';
                blackMirror.style.display = 'flex';
                blackMirror.style.opacity = '1';
                blackMirror.innerHTML = '<div class="terminal-prompt">WHAT DO YOU FEEL NOW?<span class="blink">_</span></div>';
            }, 1500);
        }

        let audioCtx = null;
        const tuner = document.getElementById('freq-tuner');
        const readout = document.getElementById('freq-readout');

        const loveStoryNotes = [220, 261.63, 329.63, 440, 392.00, 329.63, 261.63];
        let noteIdx = 0;

        function playMusicBoxNote(freq) {
            if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();
            const osc = audioCtx.createOscillator();
            const gain = audioCtx.createGain();
            
            osc.type = 'sine';
            osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
            
            gain.gain.setValueAtTime(0.3, audioCtx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 1.2);
            
            osc.connect(gain);
            gain.connect(audioCtx.destination);
            
            osc.start();
            osc.stop(audioCtx.currentTime + 1.2);
        }

        tuner.addEventListener('input', function() {
            const val = this.value;
            readout.innerText = (88.0 + (val * 0.2)).toFixed(1) + " MHz";
            
            if (val > 80 && Math.random() < 0.3) {
                playMusicBoxNote(loveStoryNotes[noteIdx % loveStoryNotes.length]);
                noteIdx++;
            }
        });

        document.getElementById('ant-node').addEventListener('click', () => {
            updateDisplay('THE STEAMPUNK ANT', 'Tethered to home by an invisible thread, exploring the infinite edge of the fabric. Follow the breadcrumbs.');
        });
    </script>
</body>
</html>
```